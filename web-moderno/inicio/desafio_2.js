// área da circunferência = PI * raio * raio

let raio = 10;
const PI = Math.PI;
let area = (PI * Math.pow(raio, 2)).toFixed(2);

console.log(`A área de uma circuferência de raio de ${raio}cm é ${area}cm²`);
